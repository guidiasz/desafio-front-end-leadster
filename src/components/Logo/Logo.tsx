import { Logo as LogoSVG } from '../Svgs/svgs';
import * as Styled from './styles';
const Logo = () => {
  return (
    <Styled.Logo title="Leadster">
      <LogoSVG />
    </Styled.Logo>
  );
};

export default Logo;

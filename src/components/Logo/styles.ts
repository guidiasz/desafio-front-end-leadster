import styled from 'styled-components';

export const LogoWithSlogan = styled.div`
  & {
    text-align: center;
    word-spacing: 1px;
  }
  & svg {
    width: 270px;
    height: auto;
    margin-bottom: 0.4rem;
  }
`;

export const Logo = styled.div`
  svg {
    display: block;
  }
`;
